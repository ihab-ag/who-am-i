window.onload = (event) =>{
    // elements
    const input= document.getElementById('input');
    const submitBtn= document.getElementById('submitBtn');
    // variables
    let name = '';

    //implementation
    

    //functions
    function getName(){
        name = input.value;
    }
}